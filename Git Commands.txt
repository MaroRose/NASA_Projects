Use this order from the repository folder:

git status
git add .
git status
git commit -m "Describe your changes"
git push origin main

If this is the first push for the branch:

git push -u origin main

Replace main with your branch name if different. If the remote may have newer changes, run this before pushing:

git pull --rebase origin main
```git pull --rebase origin main